##### Basics #####
set -g default-terminal "xterm-256color"
set-option -sa terminal-features ',*:RGB'
set -g mouse on
set -sg escape-time 10
set -g focus-events on
set -g history-limit 50000

# Start numbering at 1
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on

##### Title #####
set-option -g set-titles on
set-option -g set-titles-string "[#S] #I.#W"
set-option -g allow-rename on

##### Status Bar #####
set -g status-position bottom
set -g status-style 'bg=default,fg=default'
set -g status-interval 5

# Left side: [Session Name]
set -g status-left '[#S] '
set -g status-left-length 20

# Right side: Prefix indicator (Show "WAIT" when prefix pressed) | Date Time
set -g status-right '#{?client_prefix,WAIT ,} | %Y-%m-%d %H:%M '
set -g status-right-length 50

##### Window List Format #####
# Inactive windows: Index:ProcessName (dimmed)
setw -g window-status-style 'dim'
setw -g window-status-format '#I:#{=10:pane_current_command}#F'

# Active window: Index:ProcessName (Bold and Underlined, no color)
setw -g window-status-current-style 'bold'
setw -g window-status-current-format '#I:#{=10:pane_current_command}#F'

# Pane borders (Simple lines)
set -g pane-border-style 'fg=default,dim'
set -g pane-active-border-style 'fg=default,bold'

##### Key Bindings #####
# Reload config
bind r source-file ~/.tmux.conf \; display 'Reloaded!'

# Vim-style pane navigation
bind h select-pane -L 
bind j select-pane -D 
bind k select-pane -U 
bind l select-pane -R

# Logical splitting ( | and - )
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

# Simple detach
bind d detach

# TMUX Resurrect
run-shell $HOME/.tmux_resurrect/resurrect.tmux
